npm run electron => Runs the app on electron window only.
npm run dev => Runs the app as a web-app as well as on the electron window.

Other npm run options will need to be configured before deployment.